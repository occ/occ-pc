# TODO

## Drop temporary overrides in `occ-laptop/configuration.nix`

- [ ] **`virtualbox728`** — the `nixpkgs.overlays` block pins `virtualbox` from nixos-unstable (7.2.8, fixes kernel 7.x build). Drop the whole block once the 7.2.8 bump reaches the nixos-26.05 channel.
